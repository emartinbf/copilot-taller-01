version: "3.9"
services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: jwt-fastapi-backend
    ports:
      - "8000:8000"
    environment:
      - SECRET_KEY=super-secret-key
